{
  "name": "doubletap-targets",
  "private": true,
  "description": "Reference MCP servers the harness is tested against. The wider census set is installed separately by scripts/install.mjs, which reads scripts/candidates.servers.json; keeping several hundred dependencies in this manifest made a three-package install fail on an unrelated peer conflict.",
  "dependencies": {
    "@modelcontextprotocol/server-filesystem": "^2026.8.31",
    "@modelcontextprotocol/server-memory": "^2026.8.31",
    "@modelcontextprotocol/server-everything": "^2026.8.31"
  }
}
